# AWS Configuration
# Required for fetching database credentials and connecting to AWS services
AWS_PROFILE=personal-projects
AWS_REGION=eu-west-1

# Optional: If you don't use a profile, set these directly:
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key

# OpenAI Configuration (required for API to start)
# OPENAI_SECRET=your-openai-api-key
